import { useSelector } from 'react-redux';


export const UserDetail = () => {

    const { users } = useSelector((state) => state.users);


    return (

        <section className='secUserDetail'>

            


        </section>

    )
}
